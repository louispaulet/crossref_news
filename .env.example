# Deployment target for the frontend build.
VITE_API_BASE_URL=https://crossref-academic-news-poc.louispaulet13.workers.dev/

# API keys
# OPENAI_API_KEY=
# The Worker reads OPENAI_API_KEY locally from the repo root .env.
# For Cloudflare deploys, store it as a Worker secret with wrangler.
# CLOUDFLARE_API_TOKEN=
